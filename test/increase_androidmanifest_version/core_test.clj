(ns increase-androidmanifest-version.core-test
  (:require [clojure.test :refer :all]
            [increase-androidmanifest-version.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
